# === runtime ===
# Tkinter is bundled with the supported CPython installers. The packages below
# provide the branded image rendering and the browser-based Operations Console.
Pillow==10.4.0
streamlit==1.38.0
