# SMAI Server Analytics Web Operations Console 画面設計

## 目的

SMAI本体を変更せず、運用状態をPC、タブレット、スマートフォンから確認できる読み取り専用画面として提供する。異常を優先して表示し、投資判断、ランキング、Forecast、ユーザーデータの意味を表示・再計算しない。

## 表示構成

| 画面 | 主な内容 | 運用上の目的 |
| --- | --- | --- |
| 概要 | health score、状態カード、Service Topology、Health Timeline、L1〜L3 Check Matrix、Recovery Readiness | 最初に異常と原因層を特定する |
| 推移 | health、応答時間、容量、タスク鮮度 | 直近24時間・7日・30日の傾向を確認する |
| セッション | 端末種別ごとの現在接続・観測履歴 | heartbeatが根拠の接続状態を確認する |
| 操作履歴 | 期間、結果、ユーザー、操作による絞り込み | Runtimeの監査記録を調査する |
| 障害 | 重要度・期間の絞り込み | 失敗・要確認の発生状況を確認する |
| 改善レポート | 既存の改善提案・調査結果 | 自動化が残した調査結果を確認する |
| タスク | Schedulerの最終結果、次回予定、実行パス | 自動実行の鮮度と失敗を確認する |
| ログ | Runtimeと運用ログの末尾 | 根拠のログ行を確認する |

## 表示原則

- 状態は`healthy`、`degraded`、`critical`、`unknown`を用い、不明・破損・読み取り不能を正常として描画しない。
- 深いネイビーを基調に、シアン／ブルーを情報の強調、緑／黄／赤を状態の強調として用いる。
- 4Kと通常解像度では、画面幅をほぼ全面利用し、デスクトップではカードと表の余白・文字サイズを十分に保つ。狭い画面では列を縦に並べ替え、機能と画像を削らない。
- ブランドのワードマーク、状態マスコット、サービス構成のアイコンはカード内部へ表示し、画面幅に応じて縮小する。
- timelineの時刻ラベルは点数を抑制して重なりを避け、詳細データは推移画面で確認できるようにする。
- 画面は5秒間隔で再描画する。失敗したhealth probeは表示を停止させず、結果をfail-closedで表示する。

## 運用・ネットワーク

`run_analytics_web.bat`はWeb ConsoleをTCP 8502で起動する。LAN待受は信頼済みプライベートネットワークだけを対象とし、インターネット公開しない。画面は読み取り専用で、ユーザーデータ、secret、token、Cookie、IPアドレスを表示・記録する機能を設けない。

## 受け入れ基準

1. Overviewでブランド画像、状態、health score、Topology、Timeline、Matrix、Recoveryを同時に確認できる。
2. 8画面すべてでRuntimeの欠損・破損を正常扱いせず、`unknown`または異常として扱う。
3. 標準、degraded、critical、高負荷、復旧後の5状態を、実際のStreamlitレンダラーで検証する。
4. 起動、再起動、自動起動はいずれも`analytics_web.py`を正規入口として扱い、SMAI本体のStreamlitを停止しない。
